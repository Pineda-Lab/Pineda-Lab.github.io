---
interface Props {
  as?: "p" | "h3";
  class?: string;
}

const { as: Tag = "p", class: className } = Astro.props;
---

<Tag class:list={["text-sm font-semibold uppercase tracking-widest text-ice", className]}>
  <slot />
</Tag>
